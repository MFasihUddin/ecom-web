import React from "react";
import HeroSection from "./components/HeroSection";

function About() {
  const data = {
    name: "Fasih Ecommerece",
  };
  return <HeroSection data={data} />;
}

export default About;
